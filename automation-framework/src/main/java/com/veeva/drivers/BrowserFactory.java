package com.veeva.drivers;

import org.openqa.selenium.WebDriver;

public class BrowserFactory {

    private WebDriver driver;

    // Initialize the browser based on system property or default to Chrome
//    public WebDriver initializeBrowser(String browser) {
//        return WebDriverManagerUtil.initializeDriver(browser)
//                .orElseThrow(() -> new IllegalArgumentException("Browser not supported: " + browser));
//    }
}

